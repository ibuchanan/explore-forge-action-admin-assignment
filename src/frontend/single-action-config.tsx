import { renderActionConfig } from "./action-config";

renderActionConfig({
  name: "targetUserEmail",
  label: "Target user email",
  description:
    "Email address or Jira Automation smart value for the Target User.",
});
