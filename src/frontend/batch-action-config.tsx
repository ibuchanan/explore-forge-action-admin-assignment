import { renderActionConfig } from "./action-config";

renderActionConfig({
  name: "targetUserEmails",
  label: "Target user emails",
  description:
    "Comma-separated email addresses or a Jira Automation smart value for Target Users.",
});
